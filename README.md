# 🚀 Clean Sheets API - Vercel Backend

Backend для Clean Sheets AI расширения. Получает данные таблицы, анализирует через OpenRouter (GPT-4o-mini) и возвращает список проблем.

## 📦 Что внутри?

- **Serverless функция** на Vercel
- **OpenRouter API** для работы с GPT-4o-mini
- **CORS настройки** для работы с расширением
- **Умный промпт** для ИИ анализа данных

## 🛠️ Установка и деплой

### Шаг 1: Подготовка

1. Создайте аккаунт на [Vercel](https://vercel.com) (можно через GitHub)
2. Установите Vercel CLI (опционально):
   ```bash
   npm install -g vercel
   ```

### Шаг 2: Получите API ключ OpenRouter

1. Перейдите на [OpenRouter.ai](https://openrouter.ai/keys)
2. Создайте новый API ключ
3. Скопируйте его (начинается с `sk-or-v1-...`)

### Шаг 3: Создайте репозиторий на GitHub

1. Создайте новый репозиторий на GitHub
2. Загрузите туда всю папку `clean-sheets-api`
3. Важно: файл `.env` НЕ загружайте (он в `.gitignore`)

### Шаг 4: Деплой на Vercel

**Вариант А: Через веб-интерфейс (проще)**

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Выберите ваш GitHub репозиторий
4. Vercel автоматически определит настройки
5. Перейдите в **"Environment Variables"**:
   - Name: `OPENROUTER_API_KEY`
   - Value: ваш ключ OpenRouter
6. Нажмите **"Deploy"** 🚀
7. Через 1-2 минуты получите URL типа `https://your-app.vercel.app`

**Вариант Б: Через CLI**

```bash
# В папке clean-sheets-api
vercel

# Следуйте инструкциям
# Добавьте переменную окружения:
vercel env add OPENROUTER_API_KEY

# Введите ваш API ключ OpenRouter

# Деплой:
vercel --prod
```

### Шаг 5: Обновите расширение

После деплоя вы получите URL типа: `https://your-app.vercel.app`

1. Откройте файл `sidebar.js` в расширении
2. Найдите строку:
   ```javascript
   const API_URL = 'https://your-vercel-app.vercel.app/api/analyze';
   ```
3. Замените на ваш реальный URL:
   ```javascript
   const API_URL = 'https://your-app.vercel.app/api/analyze';
   ```
4. Перезагрузите расширение в Chrome

## 🧪 Тестирование API

Можно протестировать API напрямую через curl или Postman:

```bash
curl -X POST https://your-app.vercel.app/api/analyze \
  -H "Content-Type: application/json" \
  -d '{
    "data": [
      {"row": 1, "col": 1, "value": "  Иван Иванов  "},
      {"row": 2, "col": 1, "value": "+7999123-4567"}
    ]
  }'
```

Ответ должен быть JSON с найденными проблемами:

```json
{
  "success": true,
  "issues": [
    {
      "row": 1,
      "col": 1,
      "type": "Лишние пробелы",
      "oldValue": "  Иван Иванов  ",
      "newValue": "Иван Иванов",
      "confidence": 1.0
    },
    {
      "row": 2,
      "col": 1,
      "type": "Формат телефона",
      "oldValue": "+7999123-4567",
      "newValue": "8(999)123-45-67",
      "confidence": 0.95
    }
  ]
}
```

## 💰 Стоимость

**GPT-4o-mini** через OpenRouter:
- Input: $0.15 за 1M токенов
- Output: $0.60 за 1M токенов

**Примерный расчёт:**
- 1 таблица (~100 ячеек) ≈ 2000 токенов
- Стоимость анализа 1 таблицы ≈ $0.002 (0.2 цента)
- На $45 можно обработать ~22,500 таблиц
- Если пользователь обрабатывает по 5 таблиц = ~4,500 пользователей

**Вывод:** 2000 пользователей спокойно покроются вашими $45! 🎉

## 🔧 Настройка промпта

Если хотите изменить логику анализа, отредактируйте файл `api/analyze.js`:

```javascript
// Найдите секцию system prompt
role: 'system',
content: `Ты эксперт по очистке данных...`
```

Здесь можно:
- Добавить новые типы проблем
- Изменить форматы исправлений
- Настроить чувствительность (confidence)

## 📁 Структура

```
clean-sheets-api/
├── api/
│   └── analyze.js      # Основной endpoint
├── package.json        # Зависимости
├── vercel.json        # Конфигурация Vercel
├── .env.example       # Пример переменных
├── .gitignore         # Игнорируемые файлы
└── README.md          # Эта инструкция
```

## 🐛 Решение проблем

**Ошибка: "OPENROUTER_API_KEY is not defined"**
- Добавьте переменную окружения в Vercel настройках

**Ошибка: "CORS policy"**
- Проверьте что в `analyze.js` есть CORS заголовки
- Попробуйте переделать расширение

**Ошибка: "OpenRouter API error: 401"**
- Проверьте правильность API ключа
- Убедитесь что на балансе OpenRouter есть средства

**API не отвечает**
- Проверьте логи в Vercel Dashboard
- Убедитесь что деплой прошёл успешно

## 📊 Мониторинг

В Vercel Dashboard можете смотреть:
- 📈 Количество запросов
- ⏱️ Время выполнения функций
- ❌ Ошибки и логи
- 💰 Использование ресурсов

## 🎯 Следующие шаги

После успешного деплоя:
1. ✅ Получите URL API
2. ✅ Обновите `sidebar.js` в расширении
3. ✅ Протестируйте на реальной таблице
4. ✅ Поделитесь расширением с друзьями!

Готово! 🎉
